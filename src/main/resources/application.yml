server:
  port: 8081

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/gate_bail_users
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
  application:
    name: gatebail-user